# 脚本注释与桌宠重构计划

## 已确认的注释规范

- 范围：`Assets/Script` 下的项目脚本，排除第三方插件和生成代码。保持 UTF-8 编码，遵循 `.editorconfig`。
- 使用成对的 `#region` / `#endregion` 按功能划分；不再使用长横线分隔注释，不为每个方法单独建立区域。
- 类说明采用多行 XML 文档：第一行写中文类名，第二行以 `- ` 开头说明精炼职责。
- 每个方法（含生命周期、私有方法和协程）均有 `<summary>`；参数逐一填写 `<param name="实际参数名">`；非 void 返回值填写标准 `<returns>`，协程说明其迭代器所控制的流程。
- 文档确需分段时使用 `<para>`；需要简单换行时可用 `<br/>`。泛型参数使用 `<typeparam>`。
- Inspector 可编辑字段使用 `[Tooltip]` 说明用途、单位或约束；私有运行状态及其他重要成员使用单行 XML `<summary>`。
- 方法内部保留有价值的 `//`，解释关键分支和约束，避免逐句翻译代码。说明统一使用简体中文，标识符保持原样。
- 先核实逻辑再写注释，不把当前实现没有提供的能力描述成保证。注释整理不改变方法签名、序列化字段、数值、调用次序或游戏逻辑。

- 补充：Tooltip 仅用于实际参与 Inspector 序列化显示的字段，包括受支持的 public 实例字段及显式序列化字段；不用于属性、静态/常量/只读字段、未序列化私有缓存或不显示的字段。不为了添加 Tooltip 而改变字段访问级别或序列化方式。当前 RandomEvent 只由未序列化的私有列表持有，其内部字段使用 XML 说明。

## 注释整理进度

- 第一阶段：`Characters/Vpet/VpetAction.cs`，作为规范样板，完成后等待用户确认。
- 后续：其余 46 个脚本按功能模块整理；优先其余角色脚本，其次 Core、GameFlow、Inventory、Items，再整理 UI、Audio、Camera、World、VisualEffects。
- 核验：UTF-8 严格解码、区域配对、XML 文档及参数覆盖、排除 Tooltip 后的代码语法标记等价比较。Unity 编译和 Console 验证独立记录，不以语法检查替代编译通过。

### 第一阶段核验记录（2026-09-10）

- `VpetAction`：34 个方法的 summary、param 和 returns 校验通过；80 个字段的 XML 说明或 Tooltip 覆盖检查通过；16 个区域语法有效。
- 排除 Tooltip 属性后，修改前后 3927 个代码语法标记完全一致；XML 文档诊断为空，UTF-8 严格解码通过。
- 使用本工程当前 Bee 生成的 `Assembly-CSharp.rsp`，调用 Unity 自带 C# 编译器对整个游戏脚本程序集编译，输出重定向到系统临时目录：退出码 0，无编译诊断。
- 最新全局 Editor.log 属于另一个打开的工程，尚未取得本工程刷新后的 Console 验证；以上不等于已完成 Unity 编辑器导入及运行回归。

### 全量注释整理完成记录（2026-09-10）

- 其余 46 个脚本已按确认样式完成；连同第一阶段，`Assets/Script` 下全部 47 个脚本均已检查。本阶段不再等待逐模块确认。
- 全量覆盖：51 个类型、303 个方法、399 个字段、5 个属性、15 个枚举成员；142 个功能区域，136 个 Tooltip。方法参数与返回值文档按实际声明核对，类说明采用中文名称加精炼职责的格式。
- 全部脚本使用 UTF-8 无 BOM、CRLF；严格解码、XML 文档诊断、区域语法和 `git diff --check` 均通过。
- 与本阶段开始时的 Git HEAD 比较，排除 Tooltip 后的 21529 个代码语法标记完全一致；没有改变运行代码、序列化字段定义、资源绑定和数值。
- 使用当前工程 Bee 的 `Assembly-CSharp.rsp` 和 Unity 自带 C# 编译器重新编译整个游戏程序集，同时输出 XML 文档：退出码 0，无编译诊断。结果位于系统临时目录，不纳入项目。
- 已检查最新全局 Editor.log，但该日志仍属于另一个打开的 Unity 工程；尚未完成本工程编辑器 Console 与运行回归，不将独立程序集编译等同于编辑器完整验证。
- 用户正在移动的物品配置资源不纳入注释提交。下一阶段开始 VpetAction 职责拆分，使用下方已确认的命名和兼容边界。

## 已确认的桌宠职责拆分（尚未实施）

保留 Rigidbody2D 驱动，先完成注释整理，再分步拆分；本阶段不替换运动算法或引入新玩法。

| 类型 | 职责 |
| --- | --- |
| `VpetAction` | 保留对外入口与组件协调，逐步委托具体职责。 |
| `VpetRigMotion` | 刚体驱动、运动参数和外力处理；不用 `VpetMotor2D` 命名。 |
| `VpetEnvironmentSensor` | 地面、坡面、水域、梯子等环境探测。 |
| `VpetStateMachine` 与具体状态 | 管理状态进入、更新、退出及切换规则。 |
| `VpetAttack` | 普通攻击、跳舞攻击和攻击冷却；不用 `VpetCombat` 命名。 |
| `VpetEffect` | 食物效果、限时增益及恢复；不用 `VpetEffectController` 命名。 |
| `VpetPresentation` | 暂存表现职责；后续动画部分再整理为 `VpetAnimation`。 |

音效直接调用音频管理器方法。粒子暂时保留当前实现，未来再考虑统一的 `AddressableManager`，现在不创建该管理器。

对象池、观察者模式和策略模式列入后续计划，本轮不实现。拆分时保持现有场景、预制体绑定和外部调用兼容，并逐模块验证。

## 提交约定

提交说明使用中文，每项以 `·` 开头并换行，不添加署名或共同作者尾注。仅提交本任务改动，不夹带用户正在移动的物品配置资源。

## 职责拆分第一阶段实施记录（2026-09-10）

用户明确要求先提交整个当前工作区。本轮已将物品配置目录移动提交为 `a461986`：26 个配置及元数据文件、1 个文件夹元数据均与原内容一致，作为拆分前基线；这是对前述“不夹带资源移动”范围的本轮明确授权。

### 已完成的边界

- 新增 `VpetEnvironmentSensor`：管理接触过滤器、接触法线、接触存在判断、向下射线及瞬移重叠查询。首命中规则、接触数组上限和原有层掩码语义保持不变。
- 新增 `VpetRigMotion`：管理行走施力、攀爬、飘飞、离梯推力、直接位置设置和胶囊形状切换。保留原数值、ForceMode2D、重力补偿与速度保留规则。
- 两者暂为由 `VpetAction.Awake` 创建的普通 C# 协作对象，不是需要额外挂载的 MonoBehaviour。各操作由原入口显式调用，不新增 Update/FixedUpdate 执行顺序问题。
- `VpetAction` 仍保留 15 个序列化字段及其默认值、全部公共入口和枚举编号，无需重新挂载组件或修改场景、预制体。后续需要 Inspector 独立配置时再单独设计序列化迁移。
- 地面探测仍位于原 Update 调用点，接触过滤器仍在 Start 初始化；梯子触发回调与状态切换暂留入口。未把“碰到梯子”扩展为重叠计数等新规则。
- 本轮没有修改旧坡面算法、食物事件、战斗数值、状态转换和表现流程，也没有声称解决卡坡问题。外部炸弹、弹簧和受击施力链路未迁移。

### 验证结果与限制

- 49 个游戏脚本的 UTF-8、CRLF、XML 文档和区域语法检查通过，`git diff --check` 通过；其余 46 个脚本的运行代码与拆分前基线一致。
- `VpetAction` 的 15 个序列化声明、公共方法签名、状态枚举以及 18 个未参与此次拆分的方法保持一致。新增脚本 GUID 唯一，原脚本 GUID 未变。
- 采用 Unity 自带编译器及当前 Bee 编译参数，并显式包含新增脚本，游戏程序集与 XML 文档编译通过。编译产物放在系统临时目录，未写入 Unity 的程序集输出目录。
- 对实际新类使用记录式物理替身进行隔离回归：768 组行走参数与拆分前提取公式的施力序列一致；连同攀爬、飘飞、碰撞体、瞬移及探测规则，共通过 4135 项断言。测试不模拟 Unity 物理积分，不能替代场景回归。
- 已检查最新全局 Editor.log；该日志对应另一个打开的项目，尚无本工程刷新后的 Console 及 Play Mode 验证，不报告 Unity 完整验证通过。

### 第一阶段人工回归清单

1. 首次开场和重生后正常行走，进入水域、走坡面和接触墙面时无新增异常。
2. 接触梯子、离开梯子、空中进入梯子时，方向、速度和动画与拆分前一致。
3. 坠落、落地起身及中途进食后，持续升力正常启停；睡眠和死亡时胶囊形状正常切换。
4. 加速食物仍影响行走与飘飞施力；随机瞬移的落点查询和失败时上移行为未改变。
5. 死亡与胜利结算仍正常触发，旧预制体无 Missing Script 或引用丢失。

完成此阶段确认后，再拆分攻击、效果和表现职责，最后集中迁移状态机及各具体状态；保持各阶段可单独回退和验证。

## 职责拆分第二阶段实施记录（2026-09-10）

### 已完成的边界

- 新增 `VpetAttack`：持有普通攻击基础数值、攻击增益修正、一拳状态、普通攻击冷却和跳舞范围攻击冷却；它只向已确认的 `EnemyHealthSystem` 请求伤害，不负责动画、音效、镜头或状态切换。
- 新增 `VpetEffect`：持有加速和攻击增益的倍率与持续时间，并向 `VpetAttack` 应用或清除攻击修正。协程、飘字、音效、粒子、睡眠和随机物品事件仍由 `VpetAction` 协调。
- `VpetAction` 保留敌人层、一次性一拳图标/粒子、食物流程、跳舞音乐和玩家状态；未创建 `VpetPresentation`，也未提前迁移动画、音效或粒子资源。
- 所有新对象均由 `VpetAction.Awake` 创建，不新增需要挂载的 MonoBehaviour，不改变现有预制体或场景绑定。原有 15 个序列化字段、外部公共入口及状态枚举编号保持不变。
- 保持两个有意的旧行为：带 Enemy 标签但没有生命系统时仍会启动普通攻击冷却；攻击增益仅影响增益启动后的下一次攻击冷却，已开始的冷却不重算。跳舞攻击仍只在至少命中一名敌人后刷新冷却。
- 攻击增益粒子销毁仍沿用旧代码读取的加速增益持续时间；当前两者均为 12 秒。本阶段不借重构修正该历史行为。

### 验证结果与限制

- Unity 自带编译器使用工程当前 Bee 参数并显式包含四个新增协作类，游戏程序集与 XML 文档编译通过。
- 本工程处于打开状态时，`Library/ScriptAssemblies/Assembly-CSharp.dll` 已随本阶段源文件变更刷新；全局 Editor.log 仍由另一个打开项目写入，因此未将其作为本工程 Console 无错误的证据。
- 记录式替身回归通过 33 项断言，覆盖普通攻击、一拳状态、目标缺失时的冷却、攻击增益、加速增益、跳舞范围查询及命中冷却规则。该测试不模拟 Unity 物理、动画、协程或场景对象生命周期。
- 第二阶段完成后继续使用第一阶段的人工回归清单，并额外验证：普通攻击、一拳特效和镜头震动；可乐攻击增益的伤害与冷却；跳舞范围伤害及跳舞结束后的音乐恢复。

下一阶段仅在此阶段确认后继续：保持 `VpetPresentation` 暂不创建，先评估状态机迁移的最小兼容边界，再分批引入状态对象。

## 职责拆分第三阶段：状态机基础设施（2026-09-11）

- 新增 `VpetStateMachine`、`IVpetStateHandler`、`VpetStateBase` 及九个具体状态处理器，统一维护状态枚举、当前状态和进入/退出回调。
- `VpetAction` 不再直接持有或写入 `currentState`，所有状态读取改为 `stateMachine.CurrentState`，所有切换改为 `stateMachine.SetState`。
- 当前具体状态处理器保留为空的生命周期钩子，行为方法仍由 `VpetAction` 按原有 Unity 生命周期顺序调用。这是有意的兼容过渡：本阶段先验证状态存储与切换不破坏旧流程，下一阶段再把状态行为逐个迁入处理器。
- `VpetState` 从 `VpetAction.cs` 移到状态机脚本，枚举成员顺序和 0～8 编号不变，外部 `VpetStateSet(int)` 入口保持原签名和映射。
- 状态机是普通 C# 对象，由 `VpetAction.Awake` 创建，不添加 MonoBehaviour、Inspector 字段、场景绑定或额外 Unity 执行顺序。

### 验证

- 程序集使用当前 Unity C# 编译器与 Bee 参数编译通过，并生成 XML 文档。
- 15 个序列化字段、公共方法契约和状态枚举编号与拆分前保持一致；3 个未参与本阶段迁移的方法代码标记未变化。
- 全量注释、UTF-8、区域和 XML 覆盖检查通过；状态机新增类型和状态处理器文档完整。
- 本阶段尚未改变 Update/FixedUpdate 的实际分派，也未引入新的状态转换限制；下一步将从待机、行走、飘飞等无外部数据依赖的状态开始迁移行为。

## 职责拆分第四阶段：行走状态迁移（2026-09-11）

- `IVpetStateHandler` 增加普通帧和物理帧生命周期钩子，`VpetStateMachine` 负责向当前状态分派 `Update` 与 `FixedUpdate`。
- 新增 `VpetState_Walking`，迁入原 `VpetWalk` 的音效计时、动画参数同步、环境读取和 Rigidbody2D 行走施力逻辑。
- 行走状态通过构造函数注入 `Animator`、`VpetEnvironmentSensor`、`VpetRigMotion` 和 `VpetEffect`，不增加 Inspector 绑定，也不改变现有运动算法。
- `VpetAction` 的 `FixedUpdate` 和 `Update` 改为先交给状态机分派；其他状态行为仍保留在入口类中，确保本阶段只改变行走职责边界。
- 行走音效计时器随行走状态自身维护，原有 0.62 秒播放间隔和水面音效判断保持不变；离开行走状态时由状态退出回调关闭行走动画参数。

### 验证

- 全量脚本 UTF-8、CRLF、XML 注释、`#region`、参数和返回值覆盖检查通过。
- 序列化字段、公共入口、状态枚举编号及未迁移方法边界检查通过。
- 使用 Unity 2022.3.62f3c1 当前 Bee/Roslyn 参数编译程序集通过；Unity `Editor.log` 显示当前项目 Csc、ILPostProcess、程序集复制和域重载成功，未发现脚本编译错误。
- 下一批迁移目标为坠落与攀爬状态；会继续保留 Rigidbody2D、碰撞回调和原有协程时序。

## 状态命名与职责拆分第五阶段：攀爬状态迁移（2026-09-11）

- 所有桌宠状态类统一采用 `VpetState_xxx` 命名：`VpetState_Idle`、`VpetState_Walking`、`VpetState_Fall`、`VpetState_Climb`、`VpetState_Eat`、`VpetState_Sleep`、`VpetState_Dance`、`VpetState_Die` 和 `VpetState_Win`。
- 行走状态文件同步更名为 `VpetState_Walking.cs`，保留原 Unity GUID，避免脚本资源引用断开。
- 新增 `VpetState_Climb` 的状态生命周期实现，迁入攀爬动画参数、开始/结束动画、速度清零、随机攀爬音效和 `VpetRigMotion.Climb()` 调用。
- `VpetAction` 不再维护攀爬计时器或执行 `VpetClimb`，碰撞回调只负责请求状态切换；离开梯子时依据当前攀爬状态执行原有推离逻辑。
- 坠落状态仍保留在 `VpetAction`，因为其持续力、落地协程与进食、死亡入口存在跨职责交互，下一阶段单独处理。

## 职责拆分第六阶段：完整状态行为迁移（2026-09-11）

- 完成 `VpetState_Fall`、`VpetState_Eat`、`VpetState_Sleep`、`VpetState_Dance`、`VpetState_Die` 和 `VpetState_Win` 的行为迁移；待机状态由 `VpetState_Idle` 作为统一占位处理器保留。
- `VpetState_Fall` 接管飘飞水平施力、持续浮力、坠落音效、落地判定、起身协程和停止飘飞；`VpetAction` 仅保留地面监测与状态切换协调。
- `VpetState_Eat` 接管进食动画、食物编号分派、恢复、睡眠入口、一次性攻击、随机地球事件、增益、瞬移和飘字；睡眠与坠落之间通过显式回调协作，不直接依赖 `VpetAction` 的私有实现。
- `VpetState_Sleep` 接管睡眠协程、恢复计时、音效、动画和躺卧碰撞体切换；`VpetState_Dance` 接管跳舞期间的无敌、恢复、范围攻击、音乐淡出与结束恢复。
- `VpetState_Die` 与 `VpetState_Win` 接管结算动画、音效、碰撞体、粒子和 `GameManager` 通知；外部 `VpetDead()`、`VpetWin()` 入口及原有状态枚举保持不变。
- `VpetAction` 继续作为对外入口、Inspector 绑定、Unity 生命周期和碰撞回调协调器；状态对象通过构造函数注入依赖，避免新增挂载组件和场景绑定。
- 保留现有 `Rigidbody2D` 驱动、坡面算法、协程时序、食物编号和 Inspector 序列化字段。本阶段只完成职责迁移，不调整运动数值或引入对象池、观察者、策略及 Addressables。

### 验证

- 全量注释与编码检查通过：61 个脚本使用 UTF-8 无 BOM、CRLF，区域、XML 文档、参数返回值和 Tooltip 检查通过。
- 状态迁移边界检查通过：原有 15 个序列化字段、公共入口和状态枚举编号保持不变，未参与迁移的方法代码标记未变化，新增状态脚本 GUID 唯一。
- 使用 Unity 2022.3.62f3c1 当前工程 Bee/Roslyn 参数显式编译全部状态脚本及游戏程序集通过，并生成 XML 文档。
- Unity 批处理导入与脚本编译日志检查已完成：进程以返回码 0 退出，未发现 `error CS`、编译失败、IL 后处理失败或未处理异常；日志中的许可证客户端提示不涉及脚本编译。未进行 Play Mode 人工回归，仍需在编辑器中按历史回归清单检查状态过渡、碰撞体和动画表现。

## 对象池与Addressables统一管理阶段（2026-09-15）

- 新增 `AddressableManager`，统一提供按地址加载、Result/Handle缓存、同键并发请求合并、Label资源预加载及句柄释放能力。
- `AudioManager` 不再自行维护Addressables加载句柄和音频缓存，改为调用统一管理器；音频组的全部资源统一增加 `Audio` Label，管理器创建后会自动预加载该标签。
- 新增 `ObjectPoolManager`，按预制体维护闲置实例队列，提供获取、归还、延迟归还和对象主动归还接口。
- 伤害、恢复和增益飘字统一接入对象池；`TMP_Figure` 改为在 `OnEnable` 重置生命周期和透明度，超时后优先归还对象池，未进入对象池时保留销毁兜底。
- 暂不池化敌人、炸弹和带复杂协程/物理状态的玩法对象，待补齐统一的重置协议后再迁移，避免改变现有行为时序。

### 验证

- 使用 Unity 2022.3.62f3c1 当前工程 Bee/Roslyn 参数编译通过，并生成 XML 文档。
- Roslyn 已使用当前工程 Bee 参数编译全部游戏脚本及新增管理器，退出码为 0 且无编译警告；当前打开的本工程 `Editor.log` 未发现脚本编译错误，并记录了新脚本导入。额外 Unity 批处理因项目已被编辑器实例占用而未启动成功，未将其误记为批处理通过；未进行 Play Mode 性能对比。
